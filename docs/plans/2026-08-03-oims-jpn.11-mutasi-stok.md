# Plan: feat — Mutasi Stok (read-only ledger view)

**Issue:** oims-jpn.11 / GH-#<N>
**Date:** 2026-08-03
**Depends:** oims-jpn.5 (barang masuk), oims-jpn.8 (barang keluar) — mutasi terisi dari transaksi ini

> **PENTING:** Mutasi = append-only ledger. Baris DIBUAT oleh barang masuk/keluar/penyesuaian, BUKAN di issue ini. Issue ini = **VIEW read-only** untuk lihat riwayat pergerakan stok. TIDAK ADA create/update/delete di UI.

---

## Context

`mutasi_stok` = kartu stok / ledger append-only (PRD lanjutan). Tiap pergerakan (masuk +, keluar -, penyesuaian ±) tercatat 1 baris. Issue ini bikin halaman lihat + filter mutasi. Zero write.

Schema `mutasiStok` sudah ada: `bahanId, tipe, kuantitas (signed), barangMasukId/barangKeluarId/penyesuaianId, createdAt, createdBy`.

---

## Task 1 — Server Action listMutasi (`src/services/mutasi.ts`)

Read-only. Filter opsional: bahanId, tipe, rentang tanggal. Running balance opsional (kompleks — skip untuk MVP, cukup tampilkan kuantitas per baris + saldo dari `stok`).

```typescript
export async function listMutasi(filter?: {
  bahanId?: string; tipe?: string; from?: string; to?: string;
}) {
  await requireRole(["owner", "admin_gudang", "admin_produksi", "keuangan", "viewer"]);
  // JOIN bahan (nama, satuan), users (nama pembuat)
  // JOIN sumber dokumen: coalesce nomor dari barangMasuk/barangKeluar/penyesuaian
  // WHERE filter, ORDER BY createdAt DESC
  // pagination: LIMIT/OFFSET atau range (PostgREST cap 1000 — pakai limit eksplisit)
}
```
> Ingat aturan: paginate (default 50/page). Jangan fetch semua tanpa limit.

---

## Task 2 — Hook (`src/hooks/useMutasi.ts`)

queryKey `["mutasi", filter]`. Read-only, no mutation.

---

## Task 3 — MutasiTable + Filter (`src/components/mutasi/`)

Kolom: Tanggal, Bahan, Tipe (badge warna: masuk hijau, keluar merah, penyesuaian kuning), Kuantitas (+/- dengan warna), Nomor Dokumen sumber (link ke transaksi), Dibuat oleh.
Filter bar: pilih bahan (ComboSelect), tipe (Select), rentang tanggal (input date). Pakai DataFilter pattern kalau ada, atau simple.

---

## Task 4 — Halaman (`src/app/(with-layout)/inventory/mutasi/page.tsx`)

Server Component fetch initial + client filter. requireRole view-level.

---

## Task 5 — Verifikasi

```bash
pnpm run type-check && pnpm run build
```
Manual: setelah ada barang masuk+keluar, buka `/inventory/mutasi` → riwayat muncul urut waktu, filter jalan, badge tipe benar, link ke dokumen sumber.

---

## Commit Template
```
feat(inventory): add mutasi stok ledger view (read-only)

- listMutasi with filters (bahan/tipe/date range) + pagination
- Table with tipe badges + signed quantity + source doc link
- No write — mutasi is append-only, created by transactions

fixes #<N>
```

## CLAUDE.md Check
- [ ] Route `/inventory/mutasi`.
- [ ] Tegaskan: mutasi read-only di UI (append hanya dari transaksi).
