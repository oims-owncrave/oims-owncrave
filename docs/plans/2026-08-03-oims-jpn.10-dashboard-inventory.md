# Plan: feat — Dashboard Inventory

> **UI/CRUD standar:** ikuti [`docs/plans/_pola-master-crud.md`](./_pola-master-crud.md) — struktur file, DataTable kit, modal z-70, toast colors, soft-delete + partial unique, Checkbox. Jangan duplikasi pola di sini; plan ini fokus domain (schema, business rule).

**Issue:** oims-jpn.10 / GH-#<N>
**Date:** 2026-08-03
**Depends:** oims-jpn.9 (stok), oims-jpn.5/8 (transaksi untuk agregat)

> Dashboard = ringkasan + panel peringatan (PRD §5). Read-only aggregate. Ganti placeholder `/dashboard` "coming soon".

---

## Context

Owner butuh visibilitas tanpa buka tabel. Kartu ringkasan + panel "perlu perhatian" + (opsional) grafik sederhana.

---

## Task 1 — Server Action getDashboardStats (`src/services/dashboard.ts`)

Satu action agregat semua kartu (efisien, 1 round-trip logis):
```typescript
export async function getDashboardStats() {
  await requireRole(["owner", "admin_gudang", "admin_produksi", "keuangan", "viewer"]);
  // Hitung paralel (Promise.all):
  // - totalBahanAktif: count bahan deletedAt IS NULL, isActive
  // - totalSupplierAktif: count supplier aktif
  // - totalNilaiStok: SUM(stok.kuantitas × bahan.hargaRataRata)
  // - barangMasukBulanIni: count barang_masuk tanggal bulan berjalan
  // - barangKeluarBulanIni: count barang_keluar bulan berjalan
  // - bahanKritis: count bahan stok ≤ stokMinimum
  // - transaksiHariIni: count masuk+keluar tanggal = today
  // - penyesuaianPending: count penyesuaian status pending
  return { ... };
}

export async function getBahanKritisList() {
  // list bahan stok kritis (untuk panel perlu perhatian)
}
```

---

## Task 2 — Hook (`src/hooks/useDashboard.ts`)

queryKey `["dashboard-stats"]`, staleTime pendek (30s).

---

## Task 3 — Dashboard komponen (`src/app/(with-layout)/dashboard/_components/`)

- `StatCards.tsx` — grid kartu: total bahan, supplier, nilai stok (Rp), masuk/keluar bulan ini, bahan kritis, transaksi hari ini.
- `PerluPerhatian.tsx` — panel: bahan stok kritis (list), penyesuaian menunggu approval (count + link).
- Grafik: SKIP untuk MVP (butuh lib chart — jangan install tanpa diskusi). Placeholder "Grafik menyusul" atau tabel sederhana. Kalau mau, pakai CSS bar simple tanpa lib.

---

## Task 4 — Halaman (`src/app/(with-layout)/dashboard/page.tsx`)

REPLACE placeholder existing. Server Component fetch stats + kritis list, render cards + panel.

---

## Task 5 — Verifikasi

```bash
pnpm run type-check && pnpm run build
```
Manual: `/dashboard` → kartu angka benar sesuai data, panel kritis muncul kalau ada bahan ≤ min, link penyesuaian pending jalan.

---

## Commit Template
```
feat(dashboard): add inventory summary dashboard

- getDashboardStats aggregate (cards) + bahan kritis list
- Stat cards + perlu perhatian panel
- Replaces coming-soon placeholder

fixes #<N>
```

## CLAUDE.md Check
- [ ] Dashboard route sudah ada — update catatan status.
- [ ] Kalau kelak butuh grafik → diskusi lib dulu (jangan install diam-diam).
