# Plan: feat — Auth signin + session

**Issue:** oims-jpn.4  
**Date:** 2026-08-03  
**Status:** ✅ DONE (closed 2026-08-03)
**Scope:** Signin page minimal — hilangkan 404, user bisa login → redirect dashboard.

> **REVISI (2026-08-03):** Login diubah dari **email** → **username-only**. Supabase tetap email-based, tapi username di-map ke email sintetis `<username>@owncrave.local` di Server Action. User tak pernah lihat/ketik email. Test user superadmin (`username: superadmin`, role di `app_metadata`) dibuat via MCP. Blok kode di bawah = versi final (sudah termasuk revisi).

---

## Context

Saat ini `/` → redirect `/dashboard` → proxy deteksi no-user → redirect `/signin` → **404** (route belum ada).

Middleware (`src/proxy.ts` → `src/lib/supabase/middleware.ts`) sudah benar: redirect ke `/signin` jika no user, allow `/signin` dan `/signup`. Tidak perlu diubah.

Scope minimal (sesuai kesepakatan):
- Signin page (`/signin`) + form email/password
- Server Action `signInAction` via `supabase.auth.signInWithPassword`
- Redirect ke `/dashboard` on success, error toast on fail
- Signout action (tombol di dashboard — minimal, tanpa halaman sendiri)

**Tidak dibuat di task ini:** signup page, user management, role/hak akses (→ oims-jpn.3).

---

## Files to Create/Modify

| File | Action |
|---|---|
| `src/lib/schemas/auth.ts` | CREATE — zod schema signin |
| `src/services/auth.ts` | CREATE — Server Actions signin + signout |
| `src/app/(auth)/signin/page.tsx` | CREATE — signin page UI |
| `src/app/(auth)/layout.tsx` | CREATE — auth group layout (centered, no sidebar) |
| `src/app/dashboard/page.tsx` | MODIFY — tambah signout button |

---

## Task 1 — Zod Schema Auth

**File:** `src/lib/schemas/auth.ts`

```typescript
import { z } from "zod";

// Login pakai username saja — di-map ke email sintetis di Server Action.
export const signinSchema = z.object({
  username: z
    .string()
    .min(3, "Username minimal 3 karakter")
    .max(30, "Username maksimal 30 karakter")
    .regex(/^[a-z0-9_]+$/, "Username hanya huruf kecil, angka, underscore"),
  password: z.string().min(6, "Password minimal 6 karakter"),
});

export type SigninInput = z.infer<typeof signinSchema>;
```

**Verifikasi:** file ada, tidak ada TypeScript error (`pnpm run type-check`).

---

## Task 2 — Server Actions Auth

**File:** `src/services/auth.ts`

```typescript
"use server";

import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";

// Domain sintetis: user login pakai username, Supabase tetap simpan sebagai email.
const EMAIL_DOMAIN = "owncrave.local";

export async function signInAction(formData: { username: string; password: string }) {
  const supabase = await createClient();

  const email = `${formData.username.toLowerCase()}@${EMAIL_DOMAIN}`;

  const { error } = await supabase.auth.signInWithPassword({
    email,
    password: formData.password,
  });

  if (error) {
    return { error: "Username atau password salah" };
  }

  redirect("/dashboard");
}

export async function signOutAction() {
  const supabase = await createClient();
  await supabase.auth.signOut();
  redirect("/signin");
}
```

**Catatan:**
- `redirect()` dari `next/navigation` throw `NEXT_REDIRECT` — jangan wrap dalam try/catch yang catch-all.
- Return `{ error: string }` untuk error — client handle via toast.

**Verifikasi:** `pnpm run type-check` — tidak ada error.

---

## Task 3 — Auth Layout

**File:** `src/app/(auth)/layout.tsx`

```typescript
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center p-4">
      {children}
    </div>
  );
}
```

---

## Task 4 — Signin Page

**File:** `src/app/(auth)/signin/page.tsx`

```typescript
"use client";

import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { toast } from "sonner";
import { Button } from "@/components/ui/Button";
import { Input } from "@/components/ui/Input";
import { signinSchema, type SigninInput } from "@/lib/schemas/auth";
import { signInAction } from "@/services/auth";

export default function SigninPage() {
  const {
    register,
    handleSubmit,
    formState: { errors, isSubmitting },
  } = useForm<SigninInput>({
    resolver: zodResolver(signinSchema),
  });

  async function onSubmit(data: SigninInput) {
    const result = await signInAction(data);
    if (result?.error) {
      toast.error(result.error);
    }
  }

  return (
    <div className="w-full max-w-sm">
      <div className="bg-white rounded-2xl shadow-sm border border-gray-200 p-8">
        <div className="mb-8 text-center">
          <h1 className="text-2xl font-bold text-gray-900">OIMS Owncrave</h1>
          <p className="mt-1 text-sm text-gray-500">Masuk ke akun Anda</p>
        </div>

        <form onSubmit={handleSubmit(onSubmit)} className="flex flex-col gap-4">
          <Input
            id="username"
            type="text"
            label="Username"
            placeholder="superadmin"
            autoComplete="username"
            error={errors.username?.message}
            required
            {...register("username")}
          />
          <Input
            id="password"
            type="password"
            label="Password"
            placeholder="••••••••"
            error={errors.password?.message}
            required
            {...register("password")}
          />
          <Button
            type="submit"
            className="mt-2 w-full"
            disabled={isSubmitting}
          >
            {isSubmitting ? "Masuk..." : "Masuk"}
          </Button>
        </form>
      </div>
    </div>
  );
}
```

**Verifikasi:** `pnpm run type-check` clean.

---

## Task 5 — Signout Button di Dashboard

**File:** `src/app/dashboard/page.tsx`

Tambah signout button di pojok kanan atas. File saat ini:

```typescript
export default function DashboardPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <div className="text-center">
        <h1 className="text-2xl font-bold text-gray-900">OIMS Owncrave</h1>
        <p className="mt-2 text-gray-500">Dashboard — coming soon</p>
      </div>
    </main>
  );
}
```

Ganti dengan:

```typescript
import { Button } from "@/components/ui/Button";
import { signOutAction } from "@/services/auth";

export default function DashboardPage() {
  return (
    <main className="min-h-screen p-6">
      <div className="flex items-center justify-between mb-8">
        <h1 className="text-2xl font-bold text-gray-900">OIMS Owncrave</h1>
        <form action={signOutAction}>
          <Button type="submit" variant="outline" size="sm">
            Keluar
          </Button>
        </form>
      </div>
      <p className="text-gray-500">Dashboard — coming soon</p>
    </main>
  );
}
```

**Catatan:** `<form action={signOutAction}>` — pattern Server Action via form action, bukan `onClick` client. Dashboard page tetap Server Component.

---

## Task 6 — Verifikasi End-to-End

```bash
pnpm run type-check    # harus 0 error
pnpm run build         # harus green
```

Manual test (jalankan `pnpm dev`, buka browser):
1. Buka `http://localhost:3000` → harus redirect ke `/signin` (bukan 404)
2. Submit form kosong → validation error muncul inline
3. Submit email/password salah → toast error muncul
4. Submit kredensial valid (dari Supabase dashboard) → redirect ke `/dashboard`
5. Klik "Keluar" di dashboard → redirect ke `/signin`

---

## Commit Message Template

```
feat(auth): add signin page and session management

- Signin form with email/password validation (rhf + zod)
- signInAction and signOutAction Server Actions via Supabase
- Auth group layout (src/app/(auth)/)
- Signout button on dashboard

fixes #XX
```

*(Ganti #XX dengan GH Issue number setelah Step 4)*

---

## CLAUDE.md Check
- [ ] Apakah ada pattern/arsitektur BARU yang diperkenalkan di task ini?
  → Ya: `src/services/auth.ts` = Server Actions pattern pertama. `src/lib/schemas/` = pertama kali dipakai. Dokumentasikan di CLAUDE.md setelah implementasi.
- [ ] Apakah ada tabel database baru? → Tidak (pakai Supabase Auth bawaan)
- [ ] Route baru? → `/signin` dan `(auth)/layout.tsx` — update struktur folder di CLAUDE.md
- [ ] Permission pattern baru? → Tidak (middleware sudah handle redirect)
- [ ] Jika ada yang perlu diupdate → update CLAUDE.md setelah implementasi selesai
