# Plan: feat — Stok Bahan (read-only current view)

> **UI/CRUD standar:** ikuti [`docs/plans/_pola-master-crud.md`](./_pola-master-crud.md) — struktur file, DataTable kit, modal z-70, toast colors, soft-delete + partial unique, Checkbox. Jangan duplikasi pola di sini; plan ini fokus domain (schema, business rule).

**Issue:** oims-jpn.9 / GH-#<N>
**Date:** 2026-08-03
**Depends:** oims-jpn.7 (bahan), oims-jpn.5/8 (transaksi yang isi stok)

> **PENTING:** Stok = cache read-only. `stok.kuantitas` di-update lewat mutasi (barang masuk/keluar/penyesuaian), TIDAK PERNAH di-edit manual. Issue ini = view stok terkini + alert stok minimum. Zero write.

---

## Context

Halaman stok = snapshot kuantitas terkini per bahan + nilai persediaan + alert stok kritis (PRD §12). Read dari `stok` JOIN `bahan`.

---

## Task 1 — Server Action listStok (`src/services/stok.ts`)

```typescript
export async function listStok(filter?: { kategoriId?: string; kritisOnly?: boolean }) {
  await requireRole(["owner", "admin_gudang", "admin_produksi", "keuangan", "viewer"]);
  // SELECT bahan JOIN stok JOIN kategori JOIN satuan
  //   kode, nama, kategori, satuan, stok.kuantitas, bahan.stokMinimum, bahan.hargaRataRata
  //   nilai = kuantitas × hargaRataRata (computed di query atau di JS)
  //   isKritis = kuantitas <= stokMinimum
  // WHERE bahan.deletedAt IS NULL, filter kategori/kritis
  // ORDER BY nama
}
```

---

## Task 2 — Hook (`src/hooks/useStok.ts`)

queryKey `["stok", filter]`.

---

## Task 3 — StokTable + summary cards (`src/app/(with-layout)/inventory/stok/_components/`)

- Cards: Total jenis bahan aktif, Total nilai persediaan (Rp), Jumlah bahan stok kritis.
- Tabel: Kode, Nama, Kategori, Stok (qty + satuan), Stok Min, Nilai (Rp), Status (Normal/Kritis badge merah jika ≤ min).
- Filter: kategori, checkbox "Hanya stok kritis".

---

## Task 4 — Halaman (`src/app/(with-layout)/inventory/stok/page.tsx`)

Server Component. requireRole view-level.

---

## Task 5 — Verifikasi

```bash
pnpm run type-check && pnpm run build
```
Manual: `/inventory/stok` → qty sesuai mutasi, nilai = qty×harga, badge kritis muncul saat stok ≤ minimum, filter jalan.

---

## Commit Template
```
feat(inventory): add stok bahan current view (read-only)

- listStok with kategori/kritis filter, computed nilai persediaan
- Summary cards + table with stok kritis alert
- Read-only — stok is cache updated via mutasi

fixes #<N>
```

## CLAUDE.md Check
- [ ] Route `/inventory/stok`.
