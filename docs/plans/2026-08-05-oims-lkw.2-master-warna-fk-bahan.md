# Plan — oims-lkw.2: Master Warna + FK warnaId di bahan

**Tanggal:** 2026-08-05 · **Issue:** oims-lkw.2 (feature, P2) · **Parent:** oims-lkw
**Depends:** — · Ada schema change (2 migration)

## Context

Feedback owner: butuh master warna. Keputusan klien: **model "1 bahan = 1 warna"** — warna jadi FK atribut bahan (bagian identitas). Kain Katun Merah & Biru = 2 baris bahan berbeda. **Stok TAK berubah** (`stok.bahanId` tetap unique, mutasi/laporan tak tersentuh). Ini model paling aman — warna cuma dropdown saat buat bahan.

## Fakta terverifikasi

- Pola master CRUD: `docs/plans/_pola-master-crud.md` + `master/kategori/` (4 file: page, PageClient, Table, FormModal) + `src/services/kategori.ts` + `src/lib/schemas/kategori.ts`.
- Schema `kategori`: id, kode, nama, isActive, timestamps, deletedAt + **partial unique index** `kategori_kode_active_unique` on kode where deletedAt IS NULL (WAJIB — soft-delete safe, CLAUDE.md).
- `bahan` schema: sudah punya `kategoriId`, `satuanId` FK (pola tiru untuk warnaId). Unique index `bahan_kode_active_unique`.
- `BahanFormModal.tsx` line 151-170: ComboSelect kategoriId + satuanId (pola tiru warna). Filter `isActive || id === current`.
- Nav: `NAV_DATA` (`sidebar/data/index.ts`) item "Master Data" punya sub-items (Kategori, Satuan, Supplier, Bahan) → tambah Warna.
- PageHeader wajib tiap halaman (`docs/claude/ui-components.md`).

## Bagian 2a — Master Warna CRUD (tiru kategori)

### Schema + migration
File: `src/db/schema.ts` — tambah tabel `warna`:
```ts
export const warna = pgTable("warna", {
  id: uuid("id").primaryKey().defaultRandom(),
  kode: text("kode").notNull(),  // mis "MRH", "BRU"
  nama: text("nama").notNull(),
  isActive: boolean("is_active").notNull().default(true),
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
  deletedAt: timestamp("deleted_at", { withTimezone: true }),
}, (t) => [uniqueIndex("warna_kode_active_unique").on(t.kode).where(isNull(t.deletedAt))]);
```
Generate migration Drizzle.

### Service + schema + UI (tiru kategori PERSIS)
- `src/services/warna.ts` — listWarna, createWarna, updateWarna, softDeleteWarna. Audit log tiap CREATE/UPDATE (aksi, tabel "warna", record_id, before/after JSON). Guard partial unique (cek isNull deletedAt sebelum insert, pesan error bagus).
- `src/lib/schemas/warna.ts` — Zod (kode, nama required).
- `src/app/(with-layout)/master/warna/` — page.tsx (PageHeader "Warna" + breadcrumb) + `_components/` WarnaPageClient, WarnaTable, WarnaFormModal (tiru KategoriXxx).
- Nav: tambah `{ title: "Warna", url: "/master/warna" }` ke item "Master Data" di NAV_DATA.

## Bagian 2b — FK warnaId di bahan

### Schema + migration
File: `src/db/schema.ts` — `bahan` tambah:
```ts
warnaId: uuid("warna_id").references(() => warna.id),  // NULLABLE (bahan lama tak punya)
```
Generate migration (ADD COLUMN nullable — aman untuk data existing).

### Service + form + schema
- `src/services/bahan.ts` — include `warnaId` di createBahan/updateBahan input. List: LEFT JOIN warna, return `warnaNama`/`warnaId`.
- `src/lib/schemas/bahan.ts` — `warnaId: z.string().optional()` (nullable).
- `BahanFormModal.tsx` — tambah ComboSelect warna setelah satuan (tiru line 162-170): `warnaOptions` prop, `value={watch("warnaId")}`, filter isActive. **Opsional** (tak required).
- `BahanTable.tsx` — tambah kolom "Warna" (`renderCell` → warnaNama || "-", `mobileRole: "detail"`).
- `master/bahan/page.tsx` — fetch `listWarna()` → pass `warnaOptions` ke client → form.

## Verifikasi

1. Master warna: CRUD jalan. Hapus warna lalu buat ulang kode sama → tak crash 500 (partial unique).
2. Nav: "Warna" muncul di sidebar Master Data + bottom nav sheet Master.
3. Form bahan: dropdown warna muncul, pilih → tersimpan, tampil di tabel bahan.
4. Bahan lama (warnaId null) → list & edit tak error, kolom warna "-".
5. Stok/mutasi/laporan TAK berubah (regresi: barang masuk/keluar bahan tetap jalan).
6. Audit log tercatat saat CRUD warna + edit bahan.
7. `pnpm run type-check` + `build` clean. Migration applied.

## CLAUDE.md Check
- [ ] Tabel DB baru: `warna` → tambah ke Key Tables kalau ada daftar.
- [ ] Route baru: `/master/warna` → NAV_DATA (sudah).
- [ ] Partial unique index (soft-delete) — WAJIB, sudah di plan.
- [ ] Audit log tiap CREATE/UPDATE — sudah.

## Commit
```
feat(frontend): master warna + warna attribute on bahan

- tabel warna (CRUD, partial unique index soft-delete safe) tiru pola kategori
- FK warnaId nullable di bahan + ComboSelect di form + kolom tabel
- model 1 bahan=1 warna: stok/mutasi/laporan tak berubah
- nav: Warna masuk Master Data

refs oims-lkw.2
```
