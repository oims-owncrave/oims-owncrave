# Plan: feat — Laporan Tahap 1

**Issue:** oims-jpn.13 / GH-#<N> · **Priority:** P3
**Date:** 2026-08-03
**Depends:** oims-jpn.5/8/9/11 (data transaksi + stok + mutasi)

> Laporan = view + export (PRD lanjutan). Read-only. 5 jenis laporan. Export PDF/Excel = nice-to-have (bisa CSV dulu).

---

## Context

Klien butuh laporan: barang masuk, barang keluar, stok, mutasi, nilai persediaan. Filter periode. Export.

---

## Task 1 — Server Actions (`src/services/laporan.ts`)

Reuse query dari service masing-masing dengan filter periode:
- `laporanBarangMasuk(from, to)` — list + total nilai
- `laporanBarangKeluar(from, to)`
- `laporanStok()` — snapshot stok + nilai (reuse listStok)
- `laporanMutasi(from, to, bahanId?)` — reuse listMutasi
- `laporanNilaiPersediaan()` — nilai per kategori (SUM group by kategori)

Semua `requireRole(["owner", "keuangan", "viewer"])` minimal.

---

## Task 2 — Halaman + tab (`src/app/(with-layout)/laporan/`)

Bisa 1 halaman dengan tab, atau route per jenis (`/laporan/barang-masuk`, dst — sesuai NAV_DATA). Ikut NAV_DATA yang sudah ada (4 sub: barang-masuk, barang-keluar, stok, nilai-persediaan). Mutasi bisa gabung ke stok atau sub sendiri.

Tiap laporan: filter periode (date range) + tabel + tombol Export.

---

## Task 3 — Export

**MVP: CSV** (zero-dep, generate string, download via Blob). PDF/Excel = defer (butuh lib, diskusi dulu).
```typescript
// client-side CSV download helper (src/lib/export-csv.ts)
export function downloadCSV(filename: string, rows: Record<string, unknown>[]) {
  // header dari keys, escape, join, Blob, anchor download
}
```

---

## Task 4 — Verifikasi

```bash
pnpm run type-check && pnpm run build
```
Manual: tiap laporan tampil dengan filter periode, angka konsisten dengan data transaksi, export CSV download berisi data benar.

---

## Commit Template
```
feat(laporan): add Tahap 1 reports with CSV export

- 5 reports: barang masuk/keluar/stok/mutasi/nilai persediaan
- Period filters, CSV export (zero-dep)

fixes #<N>
```

## CLAUDE.md Check
- [ ] Routes `/laporan/*`.
- [ ] PDF/Excel export butuh lib → diskusi sebelum install.
