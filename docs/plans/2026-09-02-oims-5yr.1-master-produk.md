# Plan: Master Produk + Nav Section Produksi (oims-5yr.1)

> PRD Tahap 2 §5. Issue: `oims-5yr.1`. Epic: `oims-5yr` (Tahap 2 — Produksi, Cutting & Bundling).
> Pola UI/UX: **ikuti `docs/plans/_pola-master-crud.md`** — plan ini hanya berisi yang spesifik produk.

## Konteks

Fondasi Tahap 2: master produk (jaket, kaos, dll) yang nanti punya varian (warna×ukuran), BOM, dan PO produksi. Halaman pertama di section nav baru **PRODUKSI**.

Keputusan desain:
- `kategori`/`brand`/`jenis` = kolom **text nullable** (bukan FK) — belum ada kebutuhan filter relasional; upgrade ke master sendiri kalau owner minta. (ponytail: text dulu)
- `foto_url` = kolom disiapkan, **tanpa uploader dulu** — upload Supabase Storage menyusul kalau dibutuhkan.
- Kode produk **input manual** (seperti kategori/supplier), unik partial. Bukan auto-generate.

## Task 1 — DB Migration ⚡ CLAUDE VIA MCP (bukan Antigravity)

```sql
CREATE TABLE produk (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  kode text NOT NULL,
  nama text NOT NULL,
  kategori text,
  brand text,
  jenis text,
  deskripsi text,
  foto_url text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now(),
  deleted_at timestamptz
);
CREATE UNIQUE INDEX produk_kode_active_unique ON produk (kode) WHERE deleted_at IS NULL;
```

⚡ Dieksekusi Claude langsung saat sesi planning (sudah selesai). Antigravity mulai dari Task 3.

## Task 2 — schema.ts ⚡ CLAUDE (sudah selesai)

Tambah di `src/db/schema.ts` setelah `bahan` (section baru `─── Tahap 2: Produksi ───`):

```ts
export const produk = pgTable(
  "produk",
  {
    id: uuid("id").primaryKey().defaultRandom(),
    kode: text("kode").notNull(), // unik hanya baris aktif (partial index)
    nama: text("nama").notNull(),
    kategori: text("kategori"),
    brand: text("brand"),
    jenis: text("jenis"),
    deskripsi: text("deskripsi"),
    fotoUrl: text("foto_url"),
    isActive: boolean("is_active").notNull().default(true),
    createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
    updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
    deletedAt: timestamp("deleted_at", { withTimezone: true }),
  },
  (t) => [uniqueIndex("produk_kode_active_unique").on(t.kode).where(isNull(t.deletedAt))]
);
export type Produk = typeof produk.$inferSelect;
```

## Task 3 — Zod schema

`src/lib/schemas/produk.ts` — pola sama `src/lib/schemas/kategori.ts`:

```ts
import { z } from "zod";

export const produkSchema = z.object({
  kode: z.string().min(1, "Kode wajib diisi").max(20),
  nama: z.string().min(1, "Nama wajib diisi"),
  kategori: z.string().optional(),
  brand: z.string().optional(),
  jenis: z.string().optional(),
  deskripsi: z.string().optional(),
  isActive: z.boolean(),
});
export type ProdukFormValues = z.infer<typeof produkSchema>;
```

## Task 4 — Server Actions

`src/services/produk.ts` — copy pola `src/services/kategori.ts`:
- `listProduk()` — `WHERE deleted_at IS NULL`, order by nama.
- `createProduk(values)` — guard kode unik aktif (`isNull(deletedAt)`), insert, `writeAudit("CREATE","produk",...)`.
- `updateProduk(id, values)` — guard kode unik (exclude self), audit UPDATE dengan data_before/after.
- `softDeleteProduk(id)` — set `deleted_at`; guard: kalau nanti sudah direferensi varian/BOM/PO → tolak dengan pesan "nonaktifkan saja" (untuk sekarang cukup soft delete + audit DELETE; guard referensi ditambah di issue varian/BOM).

## Task 5 — Hook

`src/hooks/useProduk.ts` — copy pola `src/hooks/useKategori.ts` (useQuery list + mutations create/update/delete, toast sesuai `toastStyles`).

## Task 6 — Halaman `/produksi/produk`

`src/app/(with-layout)/produksi/produk/` — **copy struktur dari master kategori** (`src/app/(with-layout)/master/kategori/`):
- `page.tsx` → `listProduk()` → `<ProdukPageClient initialData />`
- `_components/ProdukPageClient.tsx`, `ProdukTable.tsx` (copy JSX `KategoriTable.tsx` persis), `ProdukFormModal.tsx`
- Kolom tabel: No. | Kode | Nama | Kategori | Brand | Jenis | Status | Aksi. Kolom nullable render `—`.
- Form modal: field sesuai Zod di atas; `deskripsi` pakai textarea; `isActive` pakai `<Checkbox label="Status Aktif">`.

## Task 7 — Nav section PRODUKSI

`src/components/layouts/sidebar/data/index.ts` — tambah section SEBELUM "ANALITIK":

```ts
{
  label: "PRODUKSI",
  items: [
    {
      title: "Produksi",
      icon: ProduksiIcon,
      items: [
        { title: "Produk", url: "/produksi/produk" },
      ],
    },
  ],
},
```

Icon: tambah `ProduksiIcon` di `src/components/layouts/sidebar/icons.tsx` (SVG gunting/scissors outline, ikuti gaya icon existing — `className` passthrough + `aria-hidden`). Bottom nav (`menu-sheet.tsx`) baca `NAV_DATA` — verifikasi section baru muncul otomatis, jangan hardcode.

## Verifikasi

1. `npx tsc --noEmit` clean, `npm run build` clean.
2. CRUD produk jalan: create (kode duplikat aktif → error message bagus), edit, hapus lalu buat ulang kode sama → sukses (partial index).
3. Audit log terisi untuk CREATE/UPDATE/DELETE.
4. Nav: section PRODUKSI muncul di sidebar + menu sheet mobile; `getPageTitle("/produksi/produk")` = "Produk".
5. Dark mode ok (token `dark:*`), tanpa arbitrary value.
