# Plan: feat — Master Kategori

**Issue:** oims-jpn.2 / GH-#2
**Date:** 2026-08-03
**Depends:** oims-jpn.4 (Auth — signin harus jalan dulu)

> **CATATAN:** Plan ini = **template pola CRUD master data**. Plan satuan (oims-jpn.1) dan supplier (oims-jpn.6) refer ke plan ini untuk struktur; hanya beda field/schema. Baca plan ini lengkap sebelum kerjakan ketiganya.

---

## Context

Master kategori = tabel referensi jenis bahan (Bahan Utama, Furing, Resleting, dst — PRD §6). Prerequisite `master bahan` (bahan wajib punya `kategori_id`).

Schema `kategori` **sudah ada** di `src/db/schema.ts:47-56` — JANGAN ubah:
```typescript
kategori: { id, kode (unique), nama, isActive, createdAt, updatedAt, deletedAt }
```

Aturan PRD §6:
- Kode unik, nama wajib
- Tidak boleh dihapus jika sudah digunakan (ada bahan pakai kategori ini) → soft delete + guard
- Dapat dinonaktifkan (`isActive` toggle)

**Scope:** ikut schema existing. PRD sebut `deskripsi` — schema TIDAK punya → skip (jangan ubah schema di issue ini).

---

## Arsitektur Pola CRUD (berlaku 3 master)

```
src/lib/schemas/<entity>.ts     → Zod schema (create/update input)
src/services/<entity>.ts        → Server Actions (list, create, update, softDelete) + audit log
src/hooks/use<Entity>.ts        → TanStack Query hooks (useQuery list, useMutation CUD)
src/app/master/<entity>/page.tsx → List page (table + tombol tambah)
src/components/<entity>/         → FormModal, Table (client components)
```

Pola dari CLAUDE.md: **DB hanya di Server Action** (bukan client). TanStack Query fetcher call Server Action. Audit log tiap CREATE/UPDATE/DELETE.

---

## Task 1 — Zod Schema

**File:** `src/lib/schemas/kategori.ts`

```typescript
import { z } from "zod";

export const kategoriSchema = z.object({
  kode: z.string().min(1, "Kode wajib diisi").max(20, "Kode maksimal 20 karakter"),
  nama: z.string().min(1, "Nama wajib diisi").max(100),
  isActive: z.boolean().default(true),
});

export type KategoriInput = z.infer<typeof kategoriSchema>;
```

---

## Task 2 — Server Actions + Audit Log

**File:** `src/services/kategori.ts`

Pattern: setiap mutation tulis ke `auditLog`. Ambil user dari Supabase session untuk `userId`.

```typescript
"use server";

import { and, eq, isNull } from "drizzle-orm";
import { db } from "@/db";
import { kategori, bahan, auditLog } from "@/db/schema";
import { createClient } from "@/lib/supabase/server";
import type { KategoriInput } from "@/lib/schemas/kategori";

async function currentUserId(): Promise<string | null> {
  const supabase = await createClient();
  const { data } = await supabase.auth.getUser();
  return data.user?.id ?? null;
}

async function writeAudit(
  aksi: string,
  recordId: string,
  before: unknown,
  after: unknown,
  userId: string | null
) {
  await db.insert(auditLog).values({
    userId: userId ?? undefined,
    aksi,
    tabel: "kategori",
    recordId,
    dataBefore: before ? JSON.stringify(before) : null,
    dataAfter: after ? JSON.stringify(after) : null,
  });
}

export async function listKategori() {
  return db
    .select()
    .from(kategori)
    .where(isNull(kategori.deletedAt))
    .orderBy(kategori.kode);
}

export async function createKategori(input: KategoriInput) {
  const userId = await currentUserId();

  // Guard kode unik (soft-deleted tidak dihitung? kode unique constraint di DB tetap berlaku global —
  // pakai cek manual untuk pesan error yang ramah)
  const existing = await db
    .select({ id: kategori.id })
    .from(kategori)
    .where(and(eq(kategori.kode, input.kode), isNull(kategori.deletedAt)))
    .limit(1);
  if (existing.length > 0) {
    return { error: `Kode "${input.kode}" sudah dipakai` };
  }

  const [row] = await db.insert(kategori).values(input).returning();
  await writeAudit("CREATE", row.id, null, row, userId);
  return { data: row };
}

export async function updateKategori(id: string, input: KategoriInput) {
  const userId = await currentUserId();
  const [before] = await db.select().from(kategori).where(eq(kategori.id, id)).limit(1);
  if (!before) return { error: "Kategori tidak ditemukan" };

  const [row] = await db
    .update(kategori)
    .set({ ...input, updatedAt: new Date() })
    .where(eq(kategori.id, id))
    .returning();
  await writeAudit("UPDATE", id, before, row, userId);
  return { data: row };
}

export async function softDeleteKategori(id: string) {
  const userId = await currentUserId();

  // Guard: tidak boleh hapus jika ada bahan pakai kategori ini
  const used = await db
    .select({ id: bahan.id })
    .from(bahan)
    .where(and(eq(bahan.kategoriId, id), isNull(bahan.deletedAt)))
    .limit(1);
  if (used.length > 0) {
    return { error: "Kategori tidak bisa dihapus — masih dipakai bahan. Nonaktifkan saja." };
  }

  const [before] = await db.select().from(kategori).where(eq(kategori.id, id)).limit(1);
  if (!before) return { error: "Kategori tidak ditemukan" };

  const [row] = await db
    .update(kategori)
    .set({ deletedAt: new Date() })
    .where(eq(kategori.id, id))
    .returning();
  await writeAudit("DELETE", id, before, row, userId);
  return { data: row };
}
```

**Catatan penting:**
- Return `{ data }` atau `{ error }` — client handle via toast (tidak throw).
- Guard soft delete: cek `bahan` referensi. Untuk supplier → cek `barangMasuk`. Untuk satuan → cek `bahan.satuanId`.
- `writeAudit` = helper per-file (belum diabstraksi global — YAGNI sampai 3+ entity terbukti butuh; nanti bisa extract ke `src/services/_audit.ts`).

---

## Task 3 — TanStack Query Hooks

**File:** `src/hooks/useKategori.ts`

```typescript
"use client";

import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import {
  listKategori,
  createKategori,
  updateKategori,
  softDeleteKategori,
} from "@/services/kategori";
import type { KategoriInput } from "@/lib/schemas/kategori";

const KEY = ["kategori"];

export function useKategoriList() {
  return useQuery({ queryKey: KEY, queryFn: () => listKategori() });
}

export function useKategoriMutation() {
  const qc = useQueryClient();
  const invalidate = () => qc.invalidateQueries({ queryKey: KEY });

  const create = useMutation({
    mutationFn: (input: KategoriInput) => createKategori(input),
    onSuccess: (res) => {
      if (res.error) return toast.error(res.error);
      toast.success("Kategori ditambahkan");
      invalidate();
    },
  });

  const update = useMutation({
    mutationFn: ({ id, input }: { id: string; input: KategoriInput }) => updateKategori(id, input),
    onSuccess: (res) => {
      if (res.error) return toast.error(res.error);
      toast.success("Kategori diperbarui");
      invalidate();
    },
  });

  const remove = useMutation({
    mutationFn: (id: string) => softDeleteKategori(id),
    onSuccess: (res) => {
      if (res.error) return toast.error(res.error);
      toast.success("Kategori dihapus");
      invalidate();
    },
  });

  return { create, update, remove };
}
```

**PREREQUISITE:** TanStack Query provider harus ada di app. Cek `src/app/layout.tsx` — saat ini BELUM ada `QueryClientProvider`. **Task tambahan (sekali saja, di master pertama yang dikerjakan):** buat `src/app/providers.tsx` dengan `QueryClientProvider` + bungkus `{children}` di layout. Master berikutnya reuse.

```typescript
// src/app/providers.tsx
"use client";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useState } from "react";

export function Providers({ children }: { children: React.ReactNode }) {
  const [client] = useState(() => new QueryClient());
  return <QueryClientProvider client={client}>{children}</QueryClientProvider>;
}
```
Di `layout.tsx`: bungkus `<Providers>{children}</Providers>` (di dalam body, membungkus children + Toaster boleh di luar).

---

## Task 4 — Table Component

**File:** `src/components/kategori/KategoriTable.tsx`

Client component. Kolom: Kode, Nama, Status (badge aktif/nonaktif), Aksi (Edit, Hapus). Pakai `ConfirmDialog` untuk hapus. Terima `data`, `onEdit`, `onDelete` sebagai props.

Styling: tabel Tailwind sederhana (`w-full`, `border-collapse`, header `bg-gray-50`, row hover `bg-gray-50`). Badge status: `isActive` → hijau "Aktif", else abu "Nonaktif".

---

## Task 5 — Form Modal

**File:** `src/components/kategori/KategoriFormModal.tsx`

Client component. Modal overlay (pola sama `ConfirmDialog`: `fixed inset-0 bg-black/50`). Isi: form rhf+zod (`kategoriSchema`), field Kode + Nama + toggle Status. Mode create/edit (kalau ada `initialData` → edit). Submit call mutation dari hook.

Pakai `Input` UI kit. Toggle status: checkbox atau switch sederhana.

---

## Task 6 — List Page

**File:** `src/app/master/kategori/page.tsx`

Client component (pakai hooks). Header "Master Kategori" + tombol "+ Tambah Kategori". State: modal open + editing row. Render `KategoriTable` + `KategoriFormModal`. Loading state saat `useKategoriList().isLoading`.

---

## Task 7 — Verifikasi

```bash
pnpm run type-check   # 0 error
pnpm run build        # green
```

Manual (butuh Auth jalan dulu — login):
1. Buka `/master/kategori` → tabel tampil (kosong awalnya)
2. Tambah kategori → muncul di tabel, toast sukses
3. Tambah kode duplikat → toast error "Kode sudah dipakai"
4. Edit → perubahan tersimpan
5. Hapus kategori tak terpakai → hilang dari tabel (soft delete, `deletedAt` terisi di DB)
6. Cek `audit_log` di DB → ada baris CREATE/UPDATE/DELETE

---

## Commit Message Template

```
feat(master): add kategori CRUD with audit log

- Zod schema + Server Actions (list/create/update/softDelete)
- TanStack Query hooks + QueryClientProvider setup
- List page, table, form modal
- Soft delete guard (block if used by bahan) + audit log

fixes #2
```

---

## CLAUDE.md Check
- [ ] Pattern BARU: CRUD master pattern (schema→service→hook→page). Dokumentasikan sebagai template di CLAUDE.md setelah 3 master jadi.
- [ ] `QueryClientProvider` + `src/app/providers.tsx` = infra baru → catat di CLAUDE.md struktur folder.
- [ ] Route baru `/master/kategori` → update struktur folder CLAUDE.md.
- [ ] `writeAudit` helper per-file → kalau di-extract ke `_audit.ts`, dokumentasikan.
