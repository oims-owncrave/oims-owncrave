# Plan: feat — Audit Log (view page)

**Issue:** oims-jpn.14 / GH-#<N> · **Priority:** P3
**Date:** 2026-08-03
**Depends:** semua fitur yang tulis audit (jpn.2/1/6/7/5/8/12/3)

> Audit log = view read-only riwayat aksi (PRD wajib, compliance). Baris DITULIS oleh tiap Server Action (`writeAudit`), issue ini cuma bikin halaman lihat. Owner-only.

---

## Context

`audit_log` terisi dari tiap CREATE/UPDATE/DELETE/APPROVE. Issue ini = halaman `/sistem/log` untuk owner lihat + filter. Zero write.

Schema `auditLog`: `userId, aksi, tabel, recordId, dataBefore(JSON), dataAfter(JSON), ipAddress, createdAt`.

---

## Task 1 — Server Action listAuditLog (`src/services/audit.ts`)

```typescript
export async function listAuditLog(filter?: {
  tabel?: string; aksi?: string; userId?: string; from?: string; to?: string;
}) {
  await requireRole(["owner"]);  // owner-only
  // JOIN users (nama pelaku), filter, ORDER BY createdAt DESC, PAGINATE (50/page)
  // dataBefore/After parse JSON untuk display diff
}
```
> Paginate wajib — audit bisa ribuan baris. PostgREST/query cap 1000.

---

## Task 2 — Hook (`src/hooks/useAuditLog.ts`)

queryKey `["audit-log", filter]`.

---

## Task 3 — AuditTable + detail (`src/components/audit/`)

- Tabel: Waktu, Pelaku, Aksi (badge), Tabel, Record ID.
- Klik baris → modal/expand tampil diff `dataBefore` vs `dataAfter` (JSON pretty).
- Filter: tabel (Select), aksi (Select), user, periode.

---

## Task 4 — Halaman (`src/app/(with-layout)/sistem/log/page.tsx`)

Server Component. `requireRole(["owner"])` + redirect kalau bukan owner (pola jpn.3 pengguna).

---

## Task 5 — Verifikasi

```bash
pnpm run type-check && pnpm run build
```
Manual: owner buka `/sistem/log` → riwayat semua aksi muncul, filter jalan, diff before/after tampil. Non-owner → redirect.

---

## Commit Template
```
feat(sistem): add audit log viewer (owner-only)

- listAuditLog with filters + pagination
- Table + before/after diff modal
- Read-only, owner-gated

fixes #<N>
```

## CLAUDE.md Check
- [ ] Route `/sistem/log`.
