CONTEXT:
Saya mengerjakan OIMS Owncrave — ERP produksi garmen, Next.js 16 + React 19 + TS strict, Tailwind v4, dark mode aktif, Drizzle + Supabase, TanStack Query, rhf+zod, sonner.

CRITICAL: Baca @CLAUDE.md untuk SEMUA coding rules.
CRITICAL: Baca plan lengkap @docs/plans/2026-09-10-oims-ckp.4-penerimaan-qc-antrean.md — bagian "Hulu" WAJIB dipahami sebelum coding.
CRITICAL: Pola aritmetika turunan: @src/lib/jahit/rekap.ts — modul QC harus meniru bentuk ini.
CRITICAL: Pola form header+detail + guard transaksi: `src/services/penerimaan-hasil-jahit.ts` dan `src/app/(with-layout)/vendor/penerimaan/`.

CATATAN: Task 1 (enum qc_prioritas + tabel penerimaan_qc + penerimaan_qc_detail + migration) SUDAH dieksekusi Claude via MCP saat planning. `src/db/schema.ts` SUDAH ter-update. Prefix "IN-QC" + DocTable "penerimaan_qc" SUDAH ditambahkan ke `src/lib/document-number.ts`. Mulai dari Task 2.

TASK:
Titik sambung Tahap 3 → Tahap 4. Terima barang hasil jahit (yang baik visual) ke QC: dokumen IN-QC-YYYYMM-NNNN + antrean QC derived. Halaman /qc/penerimaan dan /qc/antrean.

ISSUE: oims-ckp.4 (feature, P1). Blocks: oims-ckp.5.
BRANCH: feat/penerimaan-qc-antrean

KEPUTUSAN KUNCI (jangan menyimpang):
- HULU = `penerimaan_hasil_jahit_detail.jumlah_baik` = baik VISUAL, BUKAN lolos QC (komentar sudah ada di schema.ts). JANGAN UBAH TABEL TAHAP 3 sama sekali.
- `jumlah_rusak` TIDAK masuk antrean QC (sudah ditangani `selisih_jahit` Tahap 3).
- ANTREAN QC BUKAN TABEL — derived: `jumlah_baik − Σ jumlah_pcs yang sudah ke QC`. JANGAN bikin tabel antrean, JANGAN simpan kolom sisa. Ini pola referensi §4 yang dipakai seluruh Tahap 2-3.
- SKIP QC VENDOR: vendor dengan `qcMode = 'vendor'` (enum sudah ada di schema.ts:144) TIDAK muncul di antrean QC internal — QC dilakukan di tempat vendor.
- Tabel transaksi (tumbuh terus) → `useServerTable`, BUKAN client-side.

FILE:
1. `src/lib/qc/rekap.ts` — modul BARU, server-only TANPA `"use server"` (supaya bisa terima `tx`). Isi `sudahKeQcSql`:
```ts
export const sudahKeQcSql = sql<number>`(
  SELECT COALESCE(SUM(d.jumlah_pcs), 0)::int
  FROM penerimaan_qc_detail d
  JOIN penerimaan_qc p ON p.id = d.penerimaan_qc_id
  WHERE d.penerimaan_hasil_detail_id = ${h.id} AND p.deleted_at IS NULL
)`;
```
2. `src/lib/qc/prioritas.ts` — label + className prioritas (pola `src/lib/jahit/wip-status.ts`).
3. `src/lib/schemas/penerimaan-qc.ts` — Zod header + detail.
4. `src/services/penerimaan-qc.ts` — listAntreanKirimQc, listPenerimaanQc, getPenerimaanQc, createPenerimaanQc, softDeletePenerimaanQc.
5. `/qc/penerimaan/page.tsx` + `_components/`, `/qc/antrean/page.tsx` + `_components/`.
6. `src/components/layouts/sidebar/data/index.ts` — section OPERASIONAL → item "Quality Control", tambah DI ATAS "Pemeriksaan QC", TANPA disabled:
   `{ title: "Penerimaan QC", url: "/qc/penerimaan" }`, `{ title: "Antrean QC", url: "/qc/antrean" }`.
   Entri `/qc/pemeriksaan`, `/qc/rework`, `/qc/reject`, `/qc/stok-jadi` BIARKAN `disabled: true`.

GUARD WAJIB di createPenerimaanQc (ini inti issue):
Dalam SATU transaksi:
1. `generateDocNumber("IN-QC", "penerimaan_qc")` + retry-on-23505 (pola `createBarangMasuk` di `src/services/barang-masuk.ts`).
2. Untuk tiap detail: hitung ULANG sisa DI DALAM transaksi (`jumlah_baik − sudahKeQc`), JANGAN percaya angka dari client. Kalau `jumlahPcs` melebihi sisa → tolak, pesan sebut bundel + varian + sisa sebenarnya.
3. Insert header + detail.
4. `writeAudit` CREATE.

JEBAKAN:
- File `"use server"` HANYA boleh export async function — makanya `rekap.ts` & `prioritas.ts` TANPA directive itu. Ini pernah bikin `next build` gagal walau tsc lolos.
- Loop detail SEQUENTIAL (`for...of`), JANGAN `Promise.all` — race di 1 connection (pelajaran barang-masuk FIX 4).
- JANGAN masukkan objek `Date` mentah ke `sql` template raw — pernah bikin /vendor/wip crash 500 (commit 24dcb1a). Cast eksplisit atau pakai kolom Drizzle.
- Hati-hati infinite render loop di form: default `= []` inline sebagai dependency `useMemo`/`useEffect` pernah bikin form penerimaan tak bisa dipakai (commit 735bcee). Pakai konstanta di luar komponen.
- `params` async Next 16: `const { id } = await params`.
- Umur antrean dihitung dari `penerimaan_hasil_jahit.tanggal_jam`, bukan created_at dokumen QC.
- Baris vendor dengan `qcMode='vendor'` diberi badge "QC di vendor" di halaman penerimaan hasil jahit supaya operator tak bingung mencarinya di antrean.

REQUIREMENTS:
1. `pnpm exec tsc --noEmit` + `pnpm run build` clean.
2. JANGAN commit (tunggu Abu review).

VERIFIKASI — build clean BUKAN verifikasi, ini soal aritmetika stok, WAJIB query DB:
Data uji Tahap 3 masih ada (vendor VDR-0001, produk SMK, seri 9001).
1. Ambil baris ber-`jumlah_baik` > 0:
   `SELECT d.id, d.jumlah_baik FROM penerimaan_hasil_jahit_detail d JOIN penerimaan_hasil_jahit p ON p.id = d.penerimaan_id WHERE p.deleted_at IS NULL AND d.jumlah_baik > 0 LIMIT 5;`
2. Baris itu muncul di /qc/antrean dengan sisa = jumlah_baik.
3. Kirim sebagian (mis. 25 dari 30) → IN-QC-YYYYMM-0001 terbuat, antrean sisa 5. Cek:
```sql
SELECT d.jumlah_baik, COALESCE(SUM(q.jumlah_pcs),0) AS sudah_ke_qc,
       d.jumlah_baik - COALESCE(SUM(q.jumlah_pcs),0) AS sisa
FROM penerimaan_hasil_jahit_detail d
LEFT JOIN penerimaan_qc_detail q ON q.penerimaan_hasil_detail_id = d.id
LEFT JOIN penerimaan_qc h ON h.id = q.penerimaan_qc_id AND h.deleted_at IS NULL
WHERE d.id = '<id>' GROUP BY d.id, d.jumlah_baik;
```
4. GUARD: kirim 10 lagi padahal sisa 5 → DITOLAK dengan pesan jelas.
5. SKIP QC VENDOR: `UPDATE vendor SET qc_mode='vendor' WHERE kode='VDR-0001';` → baris hilang dari antrean → kembalikan ke 'internal'.
6. Audit log terisi untuk `penerimaan_qc`.
