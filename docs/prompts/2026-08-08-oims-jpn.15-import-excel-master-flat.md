CONTEXT:
Saya mengerjakan OIMS Owncrave — ERP produksi garmen, Next.js 16 + React 19 + TS strict, Tailwind v4, dark mode aktif, Drizzle + Supabase, TanStack Query, Zustand, rhf+zod, sonner toast.

CRITICAL: Baca @CLAUDE.md untuk SEMUA coding rules (soft delete, partial unique index, audit log, numeric, DB hanya di Server Action, dark mode token).
CRITICAL: Baca plan lengkap @docs/plans/2026-08-08-oims-jpn.15-import-excel-master-flat.md — ikuti PERSIS, semua snippet kode ada di sana.
CRITICAL: Baca @docs/plans/_pola-master-crud.md untuk konvensi UI (modal z-70, backdrop fixed, toast colors, Button loading).

TASK:
Bangun fitur IMPORT BATCH via upload Excel (.xlsx) untuk master data flat: kategori, satuan, supplier.
Infra generik (uploader, parser, template generator, validator, batch action all-or-nothing) + terapkan ke 3 entitas.

ISSUE: oims-jpn.15 (feature, P2).
BRANCH: feat/import-excel-master-flat

KEPUTUSAN KUNCI (jangan diubah):
- Format .xlsx (BUKAN CSV). Tambah dependency `xlsx` (SheetJS): `pnpm add xlsx`.
- Template = header kolom + 1 baris contoh. Parse + generate .xlsx di CLIENT (browser), kirim rows JSON ke Server Action.
- Import MURNI insert baru. All-or-nothing: 1 baris error → rollback SEMUA, tampilkan daftar baris gagal + alasan. Tidak ada data setengah masuk.
- Audit log per row (di dalam transaksi, pakai `tx` bukan `db`).

FILE YANG DIBUAT/DIUBAH (detail + snippet di plan):
1. `src/lib/import/types.ts` — ImportColumn, RowError, ImportResult.
2. `src/lib/import/parse-xlsx.ts` — File .xlsx → rows object (client, pakai XLSX.read + sheet_to_json header:1).
3. `src/lib/import/template.ts` — downloadTemplate() generate + download .xlsx (XLSX.writeFile).
4. `src/services/import.ts` — importKategoriBatch / importSatuanBatch / importSupplierBatch. Validasi semua row (Zod + dup dalam file + dup vs DB aktif via inArray + isNull), lalu db.transaction insert + audit per row.
5. `src/components/ui/import/ImportExcelModal.tsx` — modal reusable config-driven (download template, file input accept=".xlsx", parse, preview error, submit). Modal z-70 backdrop fixed, Button loading.
6. Per halaman (kategori/satuan/supplier): `XxxTable.tsx` tambah tombol "Import" (icon lucide `Upload`) di toolbar samping "+ Tambah" + prop `onImport`. `XxxPageClient.tsx` state `importOpen` + render `<ImportExcelModal config={...}>`.

JEBAKAN WAJIB DIPERHATIKAN (ada di plan):
- SATUAN unik-nya `nama` BUKAN kode (satuan tidak punya kode). Ganti semua cek kode→nama di importSatuanBatch.
- SUPPLIER kontak/alamat opsional → `|| null`.
- Cek duplikat vs DB HANYA baris aktif (`isNull(deletedAt)`) — partial unique. Import tidak boleh reuse kode soft-deleted (guard cukup: kalau bentrok kode aktif, tolak).
- Baris contoh di template: kolom pertama contoh isi teks "HAPUS BARIS INI" (instruksi user). Parser TIDAK auto-drop.
- Audit log dalam transaksi pakai `tx.insert(auditLog)` supaya ikut rollback.

Kolom template per entitas:
- kategori: Kode (contoh KTN), Nama (Katun).
- satuan: Nama (Meter), Singkatan (m).
- supplier: Kode (SUP-001), Nama (PT Tekstil Jaya), Kontak (0812xxx), Alamat (Bandung).

REQUIREMENTS:
1. `pnpm add xlsx` dulu.
2. Ikuti snippet kode di plan PERSIS (parser, template, batch action, modal).
3. Setelah edit: `pnpm exec tsc --noEmit` dan `pnpm run build` — harus clean.
4. JANGAN commit (tunggu Abu review).
5. JANGAN sentuh `src/lib/export-csv.ts` (fitur lain).

VERIFIKASI (lihat plan Task 7): download template tiap entitas, import file valid (data masuk + tabel refetch), import file dengan error (daftar baris gagal muncul, 0 data masuk), test duplikat dalam file, test satuan unik-by-nama, cek audit_log 1 CREATE per row.
