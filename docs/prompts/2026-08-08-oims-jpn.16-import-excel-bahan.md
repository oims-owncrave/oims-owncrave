CONTEXT:
Saya mengerjakan OIMS Owncrave — ERP produksi garmen, Next.js 16 + React 19 + TS strict, Tailwind v4, dark mode aktif, Drizzle + Supabase, TanStack Query, rhf+zod, sonner.

CRITICAL: Baca @CLAUDE.md untuk SEMUA coding rules (stok immutable, audit log, numeric, soft delete, DB hanya di Server Action).
CRITICAL: Baca plan lengkap @docs/plans/2026-08-08-oims-jpn.16-import-excel-bahan.md — snippet importBahanBatch lengkap di sana.
PREREQUISITE: oims-jpn.15 (infra import) HARUS sudah selesai & merged/ada di branch. Fitur ini NEBENG: parseXlsx, downloadTemplate, ImportExcelModal, types dari oims-jpn.15. Kalau belum ada, kerjakan oims-jpn.15 dulu.

TASK:
Perluas fitur import Excel ke entitas BAHAN. Beda dari master flat: resolve FK (kategori/satuan/warna dari teks → UUID), auto-generate kode BH-{KAT}-{NNN}, insert transaksional bahan + baris stok awal 0.

ISSUE: oims-jpn.16 (feature, P2). Depends: oims-jpn.15.
BRANCH: feat/import-excel-bahan

KEPUTUSAN KUNCI:
- User isi Excel dengan TEKS manusiawi (nama/kode kategori, nama/singkatan satuan, nama warna), BUKAN UUID. Server resolve per baris.
- Kode BAHAN auto-generate, TIDAK ada di template. Format BH-{KATEGORI_KODE}-{NNN} (lihat src/services/bahan.ts:38).
- Tiap bahan WAJIB punya baris `stok` (kuantitas 0) dalam transaksi yang sama (invariant, pola createBahan src/services/bahan.ts:55).
- All-or-nothing + audit per row (dalam transaksi).

FILE:
1. `src/services/import.ts` — tambah `importBahanBatch`. Pre-fetch kategori/satuan/warna aktif SEKALI → Map lookup. Resolve per baris (gagal = error baris). Validasi numeric stokMinimum/hargaAwal. db.transaction: generate kode (counter in-memory per kategori!), insert bahan + stok row 0 + audit.
2. `src/app/(with-layout)/master/bahan/_components/BahanTable.tsx` — tambah tombol Import (prop onImport) di toolbar samping "+ Tambah Bahan".
3. `BahanPageClient.tsx` — state importOpen + <ImportExcelModal config bahan>.

JEBAKAN KRUSIAL (ada di plan):
- KODE COUNTER IN-MEMORY: kalau 3 bahan sekategori KTN dalam 1 file, generateKodeBahan (count DB) balikin nomor SAMA untuk ketiganya → duplikat → transaksi gagal. WAJIB pakai counter Map yang increment per kategori dalam loop. Lihat snippet plan.
- Resolve kategori: match by kode ATAU nama. Satuan: match by nama ATAU singkatan. Warna: by nama, kosong = null.
- JANGAN reuse createBahan dalam loop (N transaksi). Batch = 1 transaksi, resolve/counter in-memory.
- JANGAN set stok selain baris awal 0. Stok = mutasi ledger (CLAUDE.md #1). hargaAwal → hargaRataRata; jangan seed harga lain.
- numeric: Number(string Excel) → validasi ≥0 → String() untuk Drizzle.

Kolom template bahan: Nama Bahan (req), Kategori (req, contoh KTN), Satuan (req, contoh Meter), Warna (opsional, Hitam), Stok Minimum (opsional, 10), Harga Awal (opsional, 25000). Kode TIDAK ada.

REQUIREMENTS:
1. Pastikan infra oims-jpn.15 ada (parseXlsx, ImportExcelModal, template, types).
2. Ikuti snippet importBahanBatch di plan PERSIS.
3. `pnpm exec tsc --noEmit` + `pnpm run build` clean.
4. JANGAN commit (tunggu Abu review).

VERIFIKASI (plan Task 3): import 3 bahan valid (kategori campur kode+nama, satuan pakai singkatan) → kode BH-...-001/002/003. 2 bahan sekategori dalam 1 file → kode BERURUTAN (cek eksplisit, bukan duplikat). Error resolve (kategori/satuan/warna typo) → daftar baris gagal, 0 masuk. Cek tiap bahan baru punya baris stok 0 + audit CREATE. Rollback: file 1 baris error di tengah → 0 bahan + 0 stok masuk.
