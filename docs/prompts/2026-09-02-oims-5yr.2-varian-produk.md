CONTEXT:
Saya mengerjakan OIMS Owncrave — ERP produksi garmen, Next.js 16 + React 19 + TS strict, Tailwind v4, dark mode aktif, Drizzle + Supabase, TanStack Query, rhf+zod, sonner.

CRITICAL: Baca @CLAUDE.md untuk SEMUA coding rules.
CRITICAL: Baca plan lengkap @docs/plans/2026-09-02-oims-5yr.2-varian-produk.md — snippet schema + service ada di sana.
CRITICAL: Pola UI @docs/plans/_pola-master-crud.md.
PREREQUISITE: oims-5yr.1 (master produk) HARUS sudah selesai — halaman /produksi/produk + services/produk.ts + ProdukTable ada. Kalau belum, kerjakan itu dulu.

CATATAN: Task 1 (migration varian_produk) + Task 2 (schema.ts) SUDAH dieksekusi Claude via MCP saat planning. Mulai dari Task 3.

TASK:
Varian produk = kombinasi produk × warna × ukuran + SKU auto. Dikelola dari halaman detail produk `/produksi/produk/[id]` (BUKAN route nav sendiri). Generator matrix: pilih banyak warna × banyak ukuran → semua kombinasi dibuat sekaligus.

ISSUE: oims-5yr.2 (feature, P1). Depends: oims-5yr.1.
BRANCH: feat/varian-produk

KEPUTUSAN KUNCI:
- SKU auto `[KODE_PRODUK]-[KODE_WARNA]-[UKURAN]` uppercase (contoh NJK-HTM-M), editable via edit modal. Unik partial (DB sudah enforce).
- Kombinasi (produk, warna, ukuran) unik partial — generate skip yang sudah ada, return {created, skipped[]}, toast sebutkan keduanya.
- warna = FK master warna Tahap 1 (filter aktif di MultiSelect). ukuran = text, opsi standar XS S M L XL XXL 3XL.

FILE:
1. `src/lib/schemas/varian-produk.ts` — varianGenerateSchema (warnaIds[], ukuran[], jenisKelamin?) + varianUpdateSchema (snippet plan Task 3).
2. `src/services/varian-produk.ts` — getProdukDetail, generateVarian (transaksi, pre-fetch existing → Set skip, audit per baris), updateVarian, softDeleteVarian (plan Task 4).
3. `src/hooks/useVarianProduk.ts` — query ["produk", produkId] + mutations.
4. `src/app/(with-layout)/produksi/produk/[id]/page.tsx` + `_components/`: ProdukDetailClient, VarianTable (copy KategoriTable), VarianGenerateModal (MultiSelect warna + ukuran), VarianEditModal.
5. `ProdukTable.tsx` — tambah icon action `Layers` → navigate ke detail (pakai startTransition loading, pola _pola-master-crud.md §5).

JEBAKAN:
- generateVarian: 1 transaksi untuk SEMUA kombinasi, bukan N transaksi. Pre-fetch varian existing SEKALI → cek Set `warnaId|ukuran`, jangan query per kombinasi.
- SKU dari kode warna master (warna.kode), bukan nama warna.
- MultiSelect existing di src/components/ui/ — cek API-nya dulu, jangan bikin komponen baru. Kalau tak support free-text ukuran, fallback checkbox grid + input comma-separated (plan Task 6).
- Soft delete: semua query `isNull(deletedAt)`. Audit log per varian (CREATE/UPDATE/DELETE).
- page.tsx [id]: params async di Next 16 (`const { id } = await params`).

REQUIREMENTS:
1. `pnpm exec tsc --noEmit` + `pnpm run build` clean.
2. JANGAN commit (tunggu Abu review).

VERIFIKASI (plan): generate 2 warna × 3 ukuran = 6 varian SKU benar; generate ulang → 0 created 6 skipped (toast); edit SKU duplikat → error bagus; hapus lalu generate ulang → sukses; audit terisi; getPageTitle detail = "Detail Produk".
