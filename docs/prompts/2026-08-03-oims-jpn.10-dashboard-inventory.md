CONTEXT:
Saya mengerjakan OIMS Owncrave — Next.js 16 ERP produksi garmen dengan Supabase + Drizzle backend. Admin dashboard shell + auth + user management sudah jalan.

CRITICAL: Baca @CLAUDE.md untuk SEMUA coding rules, patterns, constraints. Baca @docs/konsep-produksi.md untuk invariant domain (stok immutable, mutasi append-only, weighted average, nomor dokumen, approval).


Ikuti pola UI/CRUD standar di @docs/plans/_pola-master-crud.md (DataTable kit, modal z-70, toast semantik, checkbox, colocation). Acuan implementasi jadi: @src/app/(with-layout)/master/kategori/.
TASK:
Dashboard inventory — kartu ringkasan + panel perlu perhatian. Ganti placeholder.
Eksekusi implementation plan di @docs/plans/2026-08-03-oims-jpn.10-dashboard-inventory.md

ISSUE: oims-jpn.10
BRANCH: feat/oims-jpn.10-dashboard-inventory

⚠️ Untuk XxxTable.tsx + XxxPageClient.tsx: COPY struktur JSX dari @src/app/(with-layout)/master/kategori/_components/ PERSIS (card wrapper, icon Pencil+Trash2, `<DataTable showRowNumber />`, `<TablePagination pageSizeOptions={[10,25,50]} />`), cuma ganti nama field/label/entity. JANGAN tulis ulang tabel dari deskripsi — pasti meleset. Detail: @docs/plans/_pola-master-crud.md §4.

⚠️ JANGAN taruh <h2> di XxxPageClient.tsx — judul halaman HANYA di page.tsx (Server Component). PageClient = wrapper state saja, tanpa heading. Pattern benar:
  page.tsx: <h2>Nama Halaman</h2>  ← satu-satunya heading
  PageClient.tsx: return <div>...</div>  ← TANPA h2/h1 apapun

REQUIREMENTS:
1. Baca plan LENGKAP dulu, ikuti task-by-task berurutan.
2. Baca file sebelum edit.
- getDashboardStats aggregate (Promise.all). Grafik SKIP (jangan install lib chart tanpa diskusi).
- Replace src/app/(with-layout)/dashboard/page.tsx yang sekarang "coming soon"
7. DB hanya di Server Action. Audit log tiap CREATE/UPDATE/APPROVE. Soft delete + filter deletedAt IS NULL (untuk master).
8. Setelah semua: pnpm run type-check + pnpm run build.
9. Output per task: "✅ Task N complete: [ringkasan]".
10. JANGAN git commit/push. JANGAN ubah src/db/schema.ts.

REFERENCE FILES:
- Plan: @docs/plans/2026-08-03-oims-jpn.10-dashboard-inventory.md
- Rules: @CLAUDE.md
- Pola UI/CRUD standar: @docs/plans/_pola-master-crud.md (acuan jadi: @src/app/(with-layout)/master/kategori/)
- Konsep domain: @docs/konsep-produksi.md
- Schema: @src/db/schema.ts
- Auth: @src/lib/auth.ts
- Dashboard existing: @src/app/(with-layout)/dashboard/page.tsx

Mulai dari Task 1.
