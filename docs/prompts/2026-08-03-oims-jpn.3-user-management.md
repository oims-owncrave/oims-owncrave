CONTEXT:
Saya mengerjakan OIMS Owncrave — Next.js 16 ERP produksi garmen dengan Supabase + Drizzle backend. Admin dashboard shell sudah ada (sidebar + header + dark mode). Auth login sudah jalan (username-based, email sintetis @owncrave.local).

CRITICAL: Baca @CLAUDE.md untuk SEMUA coding rules, patterns, dan constraints.

TASK:
Eksekusi implementation plan di @docs/plans/2026-08-03-oims-jpn.3-user-management.md

ISSUE: oims-jpn.3
BRANCH: feat/oims-jpn.3-user-management

REQUIREMENTS:
1. Baca plan LENGKAP dulu — ada 10 task berurutan, jangan skip.
2. Baca file sebelum edit (terutama sidebar/index.tsx dan (with-layout)/layout.tsx sebelum modifikasi Task 9).
3. Task 1 (seed superadmin): jalankan SQL via Supabase MCP tool `execute_sql` — ambil id dari auth.users lalu insert ke public.users.
4. Task 2: buat `src/lib/auth.ts` — helper getCurrentUser + requireRole. Ini dipakai semua task berikutnya.
5. Task 3: `src/services/user.ts` — Admin API pakai `SUPABASE_SERVICE_ROLE_KEY` dari env (sudah ada di .env.local). createClient dari @supabase/supabase-js (bukan @supabase/ssr).
6. `users` tabel TIDAK punya deletedAt — gunakan isActive:false sebagai soft-disable, bukan soft delete.
7. Task 9: modifikasi sidebar (with-layout)/layout.tsx jadi async Server Component yang pass userRole ke Sidebar. Baca file-file ini dulu sebelum edit.
8. Setelah semua task: pnpm run type-check + pnpm run build.
9. Output per task: "✅ Task N complete: [ringkasan]".
10. JANGAN git commit/push — tunggu Abu.
11. JANGAN ubah src/db/schema.ts.

REFERENCE FILES:
- Plan: @docs/plans/2026-08-03-oims-jpn.3-user-management.md
- Rules: @CLAUDE.md
- Schema: @src/db/schema.ts (tabel users + userRoleEnum)
- Auth service: @src/services/auth.ts (pola signOutAction)
- Supabase server: @src/lib/supabase/server.ts
- With-layout: @src/app/(with-layout)/layout.tsx (MODIFY di Task 9)
- Sidebar: @src/components/layouts/sidebar/index.tsx (MODIFY di Task 9)
- Sidebar data: @src/components/layouts/sidebar/data/index.ts (MODIFY di Task 9)
- UI kit: @src/components/ui/Button.tsx @src/components/ui/Input.tsx @src/components/ui/Select.tsx @src/components/ui/ConfirmDialog.tsx

Mulai dari Task 1 (seed superadmin ke public.users via MCP execute_sql).
