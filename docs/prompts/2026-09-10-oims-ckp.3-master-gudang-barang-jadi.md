CONTEXT:
Saya mengerjakan OIMS Owncrave — ERP produksi garmen, Next.js 16 + React 19 + TS strict, Tailwind v4, dark mode aktif, Drizzle + Supabase, TanStack Query, rhf+zod, sonner.

CRITICAL: Baca @CLAUDE.md untuk SEMUA coding rules.
CRITICAL: Baca plan lengkap @docs/plans/2026-09-10-oims-ckp.3-master-gudang-barang-jadi.md.
CRITICAL: Pola UI @docs/plans/_pola-master-crud.md — IKUTI PERSIS. Acuan: `src/app/(with-layout)/master/kategori/`.

CATATAN: Task 1 (enum gudang_jenis + tabel gudang_barang_jadi + migration) SUDAH dieksekusi Claude via MCP saat planning. `src/db/schema.ts` SUDAH ter-update. Mulai dari Task 2.

TASK:
Master gudang/lokasi penyimpanan barang jadi (kode, nama, jenis, alamat, PIC, default, aktif). Halaman /master/gudang-jadi. Ini DIMENSI kunci stok barang jadi (varian, grade, gudang, batch) yang dipakai oims-ckp.13.

ISSUE: oims-ckp.3 (feature, P2). Blocks: oims-ckp.13.
BRANCH: feat/master-gudang-barang-jadi

KEPUTUSAN KUNCI:
- Tabel TERPISAH dari `lokasi_produksi` (itu tempat KERJA vendor, ini tempat SIMPAN stok) — jangan reuse, jangan gabung.
- Rak/bin BUKAN tabel — nanti kolom teks di `barang_jadi_detail` (.13). Jangan bikin master rak.
- Tabel client-side (`useTable` default), master baris sedikit.

FILE:
1. `src/lib/schemas/gudang-barang-jadi.ts` — create + update schema. kode: trim + uppercase, maks 20 char.
2. `src/services/gudang-barang-jadi.ts` — listGudangBarangJadi, createGudangBarangJadi, updateGudangBarangJadi, softDeleteGudangBarangJadi. Filter `isNull(deletedAt)`, guard kode duplikat aktif, `writeAudit` tiap CREATE/UPDATE/DELETE.
3. `src/hooks/useGudangBarangJadi.ts`.
4. `/master/gudang-jadi/page.tsx` + `_components/` (PageClient, Table dengan badge "Default", FormModal).
5. `src/components/layouts/sidebar/data/index.ts` — section DATA INDUK → "Master Data", tambah `{ title: "Gudang Barang Jadi", url: "/master/gudang-jadi" }`.

ATURAN isDefault — TEPAT SATU:
Set gudang baru sebagai default → default lama otomatis lepas, DALAM SATU TRANSAKSI:
```ts
await db.transaction(async (tx) => {
  if (input.isDefault) {
    await tx.update(gudangBarangJadi)
      .set({ isDefault: false, updatedAt: new Date() })
      .where(and(eq(gudangBarangJadi.isDefault, true), isNull(gudangBarangJadi.deletedAt)));
  }
  // insert/update gudang ini
});
```
Pola ini SAMA dengan aktivasi tarif berversi (`tarif_jasa_jahit`, oims-eba.4) yang sudah lolos smoke test Tahap 3 — contek dari `src/services/tarif-jasa-jahit.ts`.

JEBAKAN:
- File `"use server"` HANYA boleh export async function — label enum jenis gudang taruh di `src/lib/qc/labels.ts`.
- COPY struktur JSX `KategoriTable.tsx` PERSIS (card wrapper, `showRowNumber`, icon `Pencil`, pagination + `pageSizeOptions={[10,25,50]}`).
- Modal backdrop `fixed` bukan `absolute`.
- Checkbox isDefault/isActive pakai UI kit `<Checkbox>` + `watch`/`setValue`.
- Guard delete: gudang dipakai `stok_barang_jadi`/`barang_jadi` — tabel itu BELUM ADA. Tulis `// TODO(oims-ckp.13): tambah guard referensi stok`, jangan query tabel yang belum ada.
- Nonaktifkan default: kalau gudang default di-nonaktifkan/hapus, jangan biarkan sistem tanpa default — minimal tampilkan warning di UI.

REQUIREMENTS:
1. `pnpm exec tsc --noEmit` + `pnpm run build` clean.
2. JANGAN commit (tunggu Abu review).

VERIFIKASI (plan):
- CRUD jalan; hapus lalu buat ulang kode sama BERHASIL (bukti partial unique index).
- UJI DEFAULT: buat G1 default → buat G2 default → query harus 1 baris:
  `SELECT kode, is_default FROM gudang_barang_jadi WHERE deleted_at IS NULL AND is_default = true;`
- Audit log terisi untuk `gudang_barang_jadi`.
