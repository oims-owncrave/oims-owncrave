CONTEXT:
Saya mengerjakan OIMS Owncrave — ERP produksi garmen, Next.js 16 + React 19 + TS strict, Tailwind v4, dark mode aktif, Drizzle + Supabase, TanStack Query, rhf+zod, sonner.

CRITICAL: Baca @CLAUDE.md untuk SEMUA coding rules (soft delete + partial unique, audit log, DB hanya di Server Action).
CRITICAL: Baca plan lengkap @docs/plans/2026-09-02-oims-5yr.1-master-produk.md.
CRITICAL: Baca pola UI @docs/plans/_pola-master-crud.md — COPY struktur JSX dari master kategori PERSIS, jangan tulis ulang dari nol.

CATATAN: Task 1 (DB migration tabel produk) + Task 2 (schema.ts) SUDAH dieksekusi Claude via MCP saat planning. Tabel `produk` sudah ada di Supabase, `produk` + type `Produk` sudah ada di src/db/schema.ts. Mulai dari Task 3.

TASK:
Halaman master pertama Tahap 2: CRUD Master Produk di route baru `/produksi/produk` + section nav baru PRODUKSI.

ISSUE: oims-5yr.1 (feature, P1). Epic: oims-5yr.
BRANCH: feat/master-produk

KEPUTUSAN KUNCI:
- kategori/brand/jenis = text nullable (BUKAN FK). Kolom tabel render `—` kalau kosong.
- foto_url ada di schema tapi TANPA uploader — jangan bikin upload UI.
- Kode produk input manual (seperti kategori), unik hanya baris aktif (partial index sudah di DB).

FILE (semua copy pola master kategori — `src/app/(with-layout)/master/kategori/` + `src/services/kategori.ts` + `src/hooks/useKategori.ts`):
1. `src/lib/schemas/produk.ts` — Zod schema (snippet di plan Task 3).
2. `src/services/produk.ts` — listProduk/createProduk/updateProduk/softDeleteProduk + guard kode unik aktif + writeAudit tiap aksi.
3. `src/hooks/useProduk.ts` — TanStack Query + toastStyles.
4. `src/app/(with-layout)/produksi/produk/page.tsx` + `_components/ProdukPageClient.tsx`, `ProdukTable.tsx`, `ProdukFormModal.tsx`. Kolom: No. | Kode | Nama | Kategori | Brand | Jenis | Status | Aksi.
5. `src/components/layouts/sidebar/icons.tsx` — tambah `ProduksiIcon` (SVG gunting outline, gaya sama icon lain).
6. `src/components/layouts/sidebar/data/index.ts` — section PRODUKSI sebelum ANALITIK (snippet plan Task 7).

JEBAKAN:
- Table: COPY `KategoriTable.tsx` (card wrapper, `showRowNumber`, pagination selalu tampil, icon `Pencil` bukan `Edit2`).
- Modal backdrop `fixed` bukan absolute; submit pakai `mutateAsync` + `<Button loading>`.
- Soft delete guard: query selalu `isNull(deletedAt)`; jangan `.unique()` flat mindset — DB sudah partial.
- Bottom nav menu-sheet baca NAV_DATA otomatis — JANGAN hardcode section di menu-sheet.
- deskripsi pakai textarea; isActive pakai `<Checkbox label="Status Aktif">` UI kit.

REQUIREMENTS:
1. `pnpm exec tsc --noEmit` + `pnpm run build` clean.
2. JANGAN commit (tunggu Abu review).

VERIFIKASI (plan): CRUD jalan; kode duplikat aktif → pesan error bagus; hapus lalu buat ulang kode sama → sukses; audit log CREATE/UPDATE/DELETE terisi; section PRODUKSI muncul di sidebar + menu sheet mobile; getPageTitle("/produksi/produk") = "Produk"; dark mode pakai token.
