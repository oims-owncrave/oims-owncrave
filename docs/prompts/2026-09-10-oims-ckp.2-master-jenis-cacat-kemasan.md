CONTEXT:
Saya mengerjakan OIMS Owncrave — ERP produksi garmen, Next.js 16 + React 19 + TS strict, Tailwind v4, dark mode aktif, Drizzle + Supabase, TanStack Query, rhf+zod, sonner.

CRITICAL: Baca @CLAUDE.md untuk SEMUA coding rules.
CRITICAL: Baca plan lengkap @docs/plans/2026-09-10-oims-ckp.2-master-jenis-cacat-kemasan.md.
CRITICAL: Pola UI @docs/plans/_pola-master-crud.md — ini single source of truth, IKUTI PERSIS. Acuan hidup yang boleh dicontek struktur JSX-nya: `src/app/(with-layout)/master/kategori/`.

CATATAN: Task 1-3 (enum qc_tingkat/cacat_kategori/cacat_sumber/kemasan_jenis + tabel jenis_cacat + kemasan + migration) SUDAH dieksekusi Claude via MCP saat planning. `src/db/schema.ts` SUDAH ter-update. Mulai dari Task 4.

TASK:
Dua master data Tahap 4: Jenis Cacat (kode, nama, kategori, keparahan, sumber, dapat diperbaiki, tindakan default) dan Kemasan (kode, nama, jenis, ukuran, bahan kemasan, supplier, biaya, stok minimum). Halaman /master/jenis-cacat dan /master/kemasan.

ISSUE: oims-ckp.2 (feature, P1). Blocks: oims-ckp.1, .7, .12.
BRANCH: feat/master-jenis-cacat-kemasan

KEPUTUSAN KUNCI:
- Dua master digabung SATU issue karena bentuknya identik (CRUD murni) — kerjakan berurutan, reuse pola yang sama.
- Tabel client-side (`useTable` default) — master baris terbatas. JANGAN `useServerTable`.
- `supplierId` di kemasan REUSE tabel `supplier` Tahap 1 — jangan bikin master supplier baru.
- `biaya` & `stokMinimum` numeric (aturan proyek: uang/kuantitas tak pernah integer) → String(v) untuk Drizzle.

FILE:
1. `src/lib/schemas/jenis-cacat.ts` + `src/lib/schemas/kemasan.ts` — create + update schema (pola `src/lib/schemas/kategori.ts`). kode: trim + uppercase, maks 20 char. biaya/stokMinimum: `z.coerce.number().min(0)`.
2. `src/services/jenis-cacat.ts` + `src/services/kemasan.ts` — list/create/update/softDelete. Semua select filter `isNull(deletedAt)`. Guard kode duplikat aktif sebelum insert. `writeAudit` tiap CREATE/UPDATE/DELETE.
3. `src/hooks/useJenisCacat.ts` + `src/hooks/useKemasan.ts`.
4. `/master/jenis-cacat/page.tsx` + `_components/` (PageClient, Table, FormModal); idem `/master/kemasan/`.
5. `src/components/layouts/sidebar/data/index.ts` — section DATA INDUK → "Master Data", tambah setelah entri "Standar QC": `{ title: "Jenis Cacat", url: "/master/jenis-cacat" }` dan `{ title: "Kemasan", url: "/master/kemasan" }`. TANPA `disabled`.

JEBAKAN:
- File `"use server"` HANYA boleh export async function. Label/konstanta enum (mis. label kategori cacat untuk dropdown) taruh di `src/lib/qc/labels.ts`, JANGAN di service — ini pernah bikin `next build` gagal walau tsc lolos.
- COPY struktur JSX dari `KategoriTable.tsx` PERSIS (card wrapper, `showRowNumber`, icon `Pencil` bukan `Edit2`, pagination selalu tampil + `pageSizeOptions={[10,25,50]}`). Jangan tulis ulang dari deskripsi.
- Modal backdrop `fixed` bukan `absolute` (kalau absolute, header/sidebar nembus modal).
- Submit pakai `mutateAsync` + `<Button loading={mutation.isPending}>`.
- Dropdown supplier: filter `isActive` TAPI keep yang sedang terpilih (`_pola-master-crud.md` §5).
- Checkbox `dapatDiperbaiki` pakai UI kit `<Checkbox>` + `watch`/`setValue`, bukan native input, bukan `register`.
- Guard delete kalau direferensi: tabel `temuan_cacat` (.7) / `packing_detail` (.12) BELUM ADA. Tulis `// TODO(oims-ckp.7/.12): tambah guard referensi` — jangan bikin guard ke tabel yang belum ada (build gagal).

REQUIREMENTS:
1. `pnpm exec tsc --noEmit` + `pnpm run build` clean.
2. JANGAN commit (tunggu Abu review).

VERIFIKASI (plan):
- CRUD dua master jalan.
- BUKTI PARTIAL UNIQUE INDEX: buat kode `CC-01` → hapus → buat lagi `CC-01` → HARUS BERHASIL. Kalau 500, ada yang salah.
- Kode duplikat aktif → ditolak dengan pesan jelas (bukan stack trace).
- Audit log terisi: `SELECT aksi, tabel FROM audit_log WHERE tabel IN ('jenis_cacat','kemasan') ORDER BY created_at DESC LIMIT 10;`
