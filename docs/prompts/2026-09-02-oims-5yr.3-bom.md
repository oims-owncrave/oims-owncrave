CONTEXT:
Saya mengerjakan OIMS Owncrave — ERP produksi garmen, Next.js 16 + React 19 + TS strict, Tailwind v4, dark mode aktif, Drizzle + Supabase, TanStack Query, rhf+zod, sonner.

CRITICAL: Baca @CLAUDE.md untuk SEMUA coding rules.
CRITICAL: Baca plan lengkap @docs/plans/2026-09-02-oims-5yr.3-bom.md — semua snippet + aturan versi di sana.
CRITICAL: Pola UI @docs/plans/_pola-master-crud.md. Pola form header+detail: barang masuk (`src/app/(with-layout)/inventory/barang-masuk/`, `src/services/barang-masuk.ts`).
PREREQUISITE: oims-5yr.1 + oims-5yr.2 selesai (produk + varian ada).

CATATAN: Task 1 (migration bom + bom_detail + enum bom_status) + Task 2 (schema.ts) SUDAH dieksekusi Claude via MCP saat planning. Mulai dari Task 3.

TASK:
Bill of Materials per produk: header (nomor BOM-YYYYMM-NNNN, versi, status draft/aktif/nonaktif) + detail bahan (kuantitas, toleransi %, berlaku ukuran). Halaman /produksi/bom (list + form baru + detail).

ISSUE: oims-5yr.3 (feature, P1). Depends: oims-5yr.2.
BRANCH: feat/bom

KEPUTUSAN KUNCI:
- SATU versi aktif per produk — DB enforce partial unique. `activateBom` = transaksi: nonaktifkan aktif lama + aktifkan target + approvedBy/At + audit APPROVE.
- Edit HANYA draft. BOM aktif/nonaktif → "buat versi baru" (copy detail → draft, versi = max+1).
- Satuan TIDAK disimpan di bom_detail — derive dari bahan.satuanId (join satuan untuk singkatan).
- Nomor via `generateDocNumber("BOM", "bom")` existing (src/lib/document-number.ts) — JANGAN bikin generator baru.

FILE:
1. `src/lib/schemas/bom.ts` — bomSchema + bomDetailSchema (snippet plan Task 3).
2. `src/services/bom.ts` — listBom, getBomDetail, createBom (versi = MAX+1 per produk, bukan count), updateBom (draft only, replace details), activateBom, deactivateBom, createNewVersion, softDeleteBom (draft only). Semua audit. (plan Task 4)
3. `src/hooks/useBom.ts`.
4. `/produksi/bom/page.tsx` + `_components/` (BomPageClient, BomTable — badge status + aksi per status), `/produksi/bom/baru/page.tsx` (form useFieldArray baris bahan), `/produksi/bom/[id]/page.tsx` (read-only + aksi), `/produksi/bom/[id]/edit/page.tsx` (reuse form, draft only).
5. `src/components/layouts/sidebar/data/index.ts` — tambah { title: "BOM", url: "/produksi/bom" } di section PRODUKSI.

JEBAKAN:
- versi pakai MAX(versi)+1 per produk — count salah kalau ada versi terhapus.
- updateBom replace details = delete+insert dalam SATU transaksi (bom_detail bukan ledger, boleh — mutasi_stok yang haram).
- activateBom: guard hanya dari draft; nonaktifkan lama DULU sebelum set aktif baru (partial unique index menolak 2 aktif).
- ComboSelect bahan: filter isActive + keep terpilih (pola _pola-master-crud.md §5 FK dropdown).
- kuantitas/toleransi = numeric → string untuk Drizzle (String(v)).
- params async Next 16: `const { id } = await params`.

REQUIREMENTS:
1. `pnpm exec tsc --noEmit` + `pnpm run build` clean.
2. JANGAN commit (tunggu Abu review).

VERIFIKASI (plan): buat BOM → v1 draft nomor benar; aktifkan; versi baru → v2 draft detail ter-copy; aktifkan v2 → v1 otomatis nonaktif (hanya 1 aktif di DB); edit aktif ditolak; edit draft replace details; audit CREATE/UPDATE/APPROVE terisi; satuan tampil dari master bahan.
